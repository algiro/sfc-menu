# Dockerfile for backend Express API
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
COPY src/api ./src/api
COPY server.cjs ./

RUN npm install express cors body-parser

# Create directory for external data mount
RUN mkdir -p /app/data

EXPOSE 3001

CMD ["node", "server.cjs"]
